// This is a Server Component by default.

export default function AdminDashboardPage() {
  return (
    <div>
      <h1>Admin Dashboard - Minimal Test</h1>
      <p>If you see this, the page component itself is loading.</p>
      <p>If the 'Unsupported Server Component type: undefined' error persists, the issue is likely external to this file (e.g., cache, config, layout).</p>
    </div>
  );
}
